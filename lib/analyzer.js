

var api = (module.exports = function(tiAppXML) {
	
}).prototype;

api.addPlugin = function(plugin) {
	
};

/*
 * Application methods
 */

api.parse = function(completionCallback, errorCallback, minifyCode) {
	
};

api.addPlugin = function(plugin) {
	
};

/*
 * Plugin methods
 */

api.addEventListener = function(eventName, callback) {
	
};

api.lookupVariable = function(variableName) {
	
};

api.replaceCurrentBranch = function(newBranch) {
	
};